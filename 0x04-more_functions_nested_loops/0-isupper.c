#include "main.h"
/**
 * _isupper - checks for uppercase letters
 *@x: the value to be checked
 *Return: return 1 for uppercases else 0
 */

int _isupper(int x);
{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
