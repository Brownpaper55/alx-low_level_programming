#include "main.h"
/**
 * _isupper -  uppercase letters
 *@c: the value to be checked
 *Return: return 1 for uppercases else 0
 */

int _isupper(int c)
{

	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
